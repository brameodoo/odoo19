<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Vista de Lista Custodios -->
    <record id="view_sgs_custodian_list" model="ir.ui.view">
        <field name="name">sgs.custodian.list</field>
        <field name="model">sgs.custodian</field>
        <field name="arch" type="xml">
            <list decoration-danger="balance &lt; 0"
                  decoration-warning="pending_service_count &gt; 0"
                  decoration-success="compliance_state == 'green'">
                <field name="employee_number"/>
                <field name="name"/>
                <field name="employee_id"/>
                <field name="position"/>
                <field name="phone"/>
                <field name="initial_fund" sum="Fondo"/>
                <field name="total_deposits" sum="Depósitos"/>
                <field name="total_expenses" sum="Gastos"/>
                <field name="balance" sum="Saldo"/>
                <field name="compliance_state" widget="badge"/>
                <!-- Campos invisibles necesarios -->
                <field name="pending_service_count" column_invisible="True"/>
                <field name="currency_id" column_invisible="True"/>
            </list>
        </field>
    </record>

    <!-- Vista Kanban Custodios -->
    <record id="view_sgs_custodian_kanban" model="ir.ui.view">
        <field name="name">sgs.custodian.kanban</field>
        <field name="model">sgs.custodian</field>
        <field name="arch" type="xml">
            <kanban>
                <templates>
                    <t t-name="kanban-box">
                        <div t-attf-class="oe_kanban_global_click o_kanban_record
                            #{record.balance.raw_value &lt; 0 and 'bg-danger' or
                              record.compliance_state.raw_value == 'green' and 'bg-success' or
                              record.compliance_state.raw_value == 'yellow' and 'bg-warning' or
                              'bg-info'}">
                            <div class="o_kanban_primary">
                                <field name="name"/>
                                <field name="employee_id"/>
                                <field name="balance"/>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- Formulario de Servicios -->
    <record id="view_sgs_service_form" model="ir.ui.view">
        <field name="name">sgs.route.service.form</field>
        <field name="model">sgs.route.service</field>
        <field name="arch" type="xml">
            <form string="Servicio / Gasto">
                <sheet>
                    <group string="Desglose de Gastos">
                        <group>
                            <field name="amount_perdiem"/>
                            <field name="amount_fuel"/>
                            <field name="amount_lodging"/>
                            <field name="amount_misc"/>
                            <field name="misc_detail"/>
                        </group>
                        <group>
                            <field name="amount_tolls" readonly="1"/>
                            <field name="amount_total" readonly="1"/>
                            <field name="currency_id" invisible="1"/>
                            <!-- Limpieza: widget="image" sin filename -->
                            <field name="evidence_image" widget="image"/>
                            <field name="evidence_filename" invisible="1"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Casetas">
                            <field name="toll_line_ids">
                                <list editable="bottom">
                                    <field name="name"/>
                                    <!-- Usa el campo correcto definido en sgs.toll.line -->
                                    <field name="amount"/>
                                    <field name="currency_id" column_invisible="True"/>
                                    <field name="image" widget="image"/>
                                    <field name="image_filename" invisible="1"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>
</odoo>
